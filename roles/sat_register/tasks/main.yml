---
- name: Install Satellite Certificate
  yum:
    name: '{{ sat_url }}'
    state: present

- name: Register to Satellite
  redhat_subscription:
    state: present
    activationkey: '{{ activate_key }}'
    org_id: '{{ org }}'
